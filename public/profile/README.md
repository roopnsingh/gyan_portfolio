# Profile Image

Your hero profile photo is loaded from:

`public/images/profile.PNG`

(Also supported here as `profile.png`, `profile.jpg`, etc. if you prefer this folder.)

Recommended: square crop, at least 400×400px (800×800px ideal).

If the file is missing, a placeholder is shown automatically.
